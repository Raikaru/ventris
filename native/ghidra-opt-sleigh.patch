diff --git a/decompiler/Makefile b/decompiler/Makefile
index 61ebba3..ee3b0e5 100644
--- a/decompiler/Makefile
+++ b/decompiler/Makefile
@@ -260,11 +260,11 @@ decomp_test_dbg:	$(TEST_DEBUG_OBJS)
 test: decomp_test_dbg
 	./decomp_test_dbg
 
-ghidra_dbg:	$(GHIDRA_DBG_OBJS)
-	$(CXX) $(DBG_CXXFLAGS) $(ADDITIONAL_FLAGS) $(MAKE_STATIC) $(ARCH_TYPE) -o ghidra_dbg $(GHIDRA_DBG_OBJS)
+ghidra_dbg:	$(GHIDRA_DBG_OBJS) $(SLEIGH:%=ghi_opt/%.o) ghi_opt/inject_sleigh.o
+	$(CXX) $(DBG_CXXFLAGS) $(ADDITIONAL_FLAGS) $(MAKE_STATIC) $(ARCH_TYPE) -o ghidra_dbg $(GHIDRA_DBG_OBJS) $(SLEIGH:%=ghi_opt/%.o) ghi_opt/inject_sleigh.o -lz
 
-ghidra_opt:	$(GHIDRA_OPT_OBJS)
-	$(CXX) $(OPT_CXXFLAGS) $(ADDITIONAL_FLAGS) $(MAKE_STATIC) $(ARCH_TYPE)  -o ghidra_opt $(GHIDRA_OPT_OBJS)
+ghidra_opt:	$(GHIDRA_OPT_OBJS) $(SLEIGH:%=ghi_opt/%.o) ghi_opt/inject_sleigh.o
+	$(CXX) $(OPT_CXXFLAGS) $(ADDITIONAL_FLAGS) $(MAKE_STATIC) $(ARCH_TYPE)  -o ghidra_opt $(GHIDRA_OPT_OBJS) $(SLEIGH:%=ghi_opt/%.o) ghi_opt/inject_sleigh.o -lz
 
 sleigh_dbg:	$(SLEIGH_DBG_OBJS)
 	$(CXX) $(DBG_CXXFLAGS) $(ADDITIONAL_FLAGS) $(MAKE_STATIC) $(ARCH_TYPE) -o sleigh_dbg $(SLEIGH_DBG_OBJS) $(LNK)
diff --git a/decompiler/consolemain.cc b/decompiler/consolemain.cc
index 87c13b8..b1b59a7 100644
--- a/decompiler/consolemain.cc
+++ b/decompiler/consolemain.cc
@@ -58,7 +58,11 @@ void IfcLoadFile::execute(istream &s)
   else
     target = "default";
 
-  ArchitectureCapability *capa = ArchitectureCapability::findCapability(filename);
+  ArchitectureCapability *capa = (ArchitectureCapability *)0;
+  if (target.find(':') != string::npos)
+    capa = ArchitectureCapability::getCapability("raw");
+  if (capa == (ArchitectureCapability *)0)
+    capa = ArchitectureCapability::findCapability(filename);
   if (capa == (ArchitectureCapability *)0)
     throw IfaceExecutionError("Unable to recognize imagefile "+filename);
   dcp->conf = capa->buildArchitecture(filename,target,status->optr);
diff --git a/decompiler/database_ghidra.cc b/decompiler/database_ghidra.cc
index 7e7e59c..b983096 100644
--- a/decompiler/database_ghidra.cc
+++ b/decompiler/database_ghidra.cc
@@ -34,6 +34,19 @@ ScopeGhidra::ScopeGhidra(ArchitectureGhidra *g)
   cacheDirty = false;
 }
 
+Symbol *ScopeGhidra::addMapSymToCache(Decoder &decoder) const
+
+{
+  return cache->addMapSym(decoder);
+}
+
+void ScopeGhidra::renameFunction(FunctionSymbol *sym,const string &newname) const
+
+{
+  cache->renameSymbol(sym,newname);
+  sym->getFunction()->setName(newname);
+}
+
 ScopeGhidra::~ScopeGhidra(void)
 
 {
diff --git a/decompiler/database_ghidra.hh b/decompiler/database_ghidra.hh
index 7b8c5f8..6b355a5 100644
--- a/decompiler/database_ghidra.hh
+++ b/decompiler/database_ghidra.hh
@@ -66,6 +66,8 @@ public:
   /// can reset to it before decompiling a new function.
   void lockDefaultProperties(void) { flagbaseDefault = ghidra->symboltab->getProperties(); cacheDirty = false; }
   virtual ~ScopeGhidra(void);
+  Symbol *addMapSymToCache(Decoder &decoder) const;
+  void renameFunction(FunctionSymbol *sym,const string &newname) const;
   virtual void clear(void);
   virtual SymbolEntry *addSymbol(const string &nm,Datatype *ct,
 				 const Address &addr,const Address &usepoint);
diff --git a/decompiler/funcdata.hh b/decompiler/funcdata.hh
index db85597..1e9529c 100644
--- a/decompiler/funcdata.hh
+++ b/decompiler/funcdata.hh
@@ -137,7 +137,8 @@ class Funcdata {
 public:
   Funcdata(const string &nm,const string &disp,Scope *conf,const Address &addr,FunctionSymbol *sym,int4 sz=0);	///< Constructor
   ~Funcdata(void);							///< Destructor
-  const string &getName(void) const { return name; }			///< Get the function's local symbol name
+  const string &getName(void) const { return name; }
+  void setName(const string &nm) { name = nm; displayName = nm; }	///< Rename the function for output			///< Get the function's local symbol name
   const string &getDisplayName(void) const { return displayName; }	///< Get the name to display in output
   const Address &getAddress(void) const { return baseaddr; }		///< Get the entry point address
   int4 getSize(void) const { return size; }				///< Get the function body size in bytes
diff --git a/decompiler/ghidra_arch.cc b/decompiler/ghidra_arch.cc
index 813d955..6296c29 100644
--- a/decompiler/ghidra_arch.cc
+++ b/decompiler/ghidra_arch.cc
@@ -21,6 +21,7 @@
 #include "comment_ghidra.hh"
 #include "string_ghidra.hh"
 #include "cpool_ghidra.hh"
+#include "sleigh.hh"
 #include "inject_ghidra.hh"
 
 namespace ghidra {
@@ -295,6 +296,21 @@ PcodeInjectLibrary *ArchitectureGhidra::buildPcodeInjectLibrary(void)
 Translate *ArchitectureGhidra::buildTranslator(DocumentStorage &store)
 
 {
+  const char *slafile = getenv("VENTRIS_SLA");
+  if ((slafile != (const char *)0) && (slafile[0] != '\0')) {
+    // Raw-SLEIGH mode (spike): register the <sleigh> tag whose CONTENT is
+    // the compiled .sla path (same handshake as
+    // SleighArchitecture::buildSpecFile, sleigh_arch.cc:410-418), then
+    // build a self-disassembling Sleigh translator instead of the
+    // client-driven GhidraTranslate. The slave instruction bytes come from
+    // the client via getBytes (LoadImageGhidra), so the worker's
+    // program-provider serves both bytes and pcode generation.
+    istringstream s(string("<sleigh>") + slafile + "</sleigh>");
+    Document *doc = store.parseDocument(s);
+    store.registerTag(doc->getRoot());
+    Sleigh *trans = new Sleigh(loader, context);
+    return trans;
+  }
   return new GhidraTranslate(this);
 }
 
@@ -369,7 +385,15 @@ void ArchitectureGhidra::buildConstantPool(DocumentStorage &store)
 void ArchitectureGhidra::buildContext(DocumentStorage &store)
 
 {
-  context = new ContextGhidra(this);
+  const char *slafile = getenv("VENTRIS_SLA");
+  if ((slafile != (const char *)0) && (slafile[0] != '\0')) {
+    // Raw-SLEIGH mode: the Sleigh self-disassembler needs a real context
+    // database (ContextGhidra blocks registerVariable).
+    context = new ContextInternal();
+  }
+  else {
+    context = new ContextGhidra(this);
+  }
 }
 
 void ArchitectureGhidra::buildSymbols(DocumentStorage &store)
diff --git a/decompiler/ghidra_process.cc b/decompiler/ghidra_process.cc
index a93aa00..9ba9404 100644
--- a/decompiler/ghidra_process.cc
+++ b/decompiler/ghidra_process.cc
@@ -14,6 +14,7 @@
  * limitations under the License.
  */
 #include "ghidra_process.hh"
+#include "database_ghidra.hh"
 #include "flow.hh"
 #include "blockaction.hh"
 
@@ -170,6 +171,7 @@ void RegisterProgram::loadParameters(void)
   ArchitectureGhidra::readStringStream(sin,cspec);
   ArchitectureGhidra::readStringStream(sin,tspec);
   ArchitectureGhidra::readStringStream(sin,corespec);
+  ArchitectureGhidra::readStringStream(sin,functionShell);
 }
 
 
@@ -192,6 +194,24 @@ void RegisterProgram::rawAction(void)
 
   DocumentStorage store;	// temp storage of initialization xml docs
   ghidra->init(store);
+
+  if (!functionShell.empty()) {
+    istringstream input(functionShell);
+    PackedDecode decoder(ghidra);
+    decoder.ingestStream(input);
+    uint4 docId = decoder.openElement(ELEM_DOC);
+    while (decoder.peekElement() != 0) {
+      uint4 elemId = decoder.peekElement();
+      if (elemId != ELEM_MAPSYM)
+	throw LowlevelError("Unknown function shell tag");
+      ScopeGhidra *scope = dynamic_cast<ScopeGhidra *>(ghidra->symboltab->getGlobalScope());
+      if (scope == (ScopeGhidra *)0)
+	throw LowlevelError("Global scope is not Ghidra-backed");
+      scope->addMapSymToCache(decoder);
+    }
+    decoder.closeElement(docId);
+  }
+
   if (open == -1) {
     open = archlist.size();
     archlist.push_back((ArchitectureGhidra *)0);
@@ -209,6 +229,49 @@ void RegisterProgram::sendResult(void)
   GhidraCommand::sendResult();
 }
 
+void SetFunctionSignature::loadParameters(void)
+
+{
+  GhidraCommand::loadParameters();
+  ArchitectureGhidra::readStringStream(sin,functionName);
+  ArchitectureGhidra::readStringStream(sin,signature);
+  ArchitectureGhidra::readStringStream(sin,functionAddress);
+}
+
+void SetFunctionSignature::rawAction(void)
+
+{
+  istringstream input(signature);
+  PrototypePieces pieces;
+  parse_protopieces(pieces,input,ghidra);
+  string requestedName = pieces.name;
+  if (functionName.empty())
+    functionName = requestedName;
+  if (functionName.empty())
+    throw ParseError("Prototype has no function name");
+  if (requestedName.empty()) {
+    requestedName = functionName;
+    pieces.name = functionName;
+  }
+  istringstream addressInput(functionAddress);
+  uintb offset = 0;
+  addressInput >> hex >> offset;
+  if (addressInput.fail())
+    throw ParseError("Bad function address: " + functionAddress);
+  Address functionAddressValue(ghidra->getDefaultCodeSpace(),offset);
+  Funcdata *fd = ghidra->symboltab->getGlobalScope()->findFunction(functionAddressValue);
+  if (fd == (Funcdata *)0)
+    throw ParseError("Unknown function address: " + functionAddress);
+  fd->getFuncProto().setPieces(pieces);
+  if (requestedName != functionName) {
+    ScopeGhidra *ghidraScope = dynamic_cast<ScopeGhidra *>(ghidra->symboltab->getGlobalScope());
+    if (ghidraScope == (ScopeGhidra *)0)
+      throw LowlevelError("Global scope is not Ghidra-backed");
+    ghidraScope->renameFunction(fd->getSymbol(),requestedName);
+  }
+  ghidra->clearAnalysis(fd);
+}
+
 void DeregisterProgram::loadParameters(void)
 
 {
@@ -497,6 +560,7 @@ void GhidraDecompCapability::initialize(void)
 
 {
   commandmap["registerProgram"] = new RegisterProgram();
+  commandmap["setFunctionSignature"] = new SetFunctionSignature();
   commandmap["deregisterProgram"] = new DeregisterProgram();
   commandmap["flushNative"] = new FlushNative();
   commandmap["decompileAt"] = new DecompileAt();
diff --git a/decompiler/ghidra_process.hh b/decompiler/ghidra_process.hh
index db72be5..a6b4bd2 100644
--- a/decompiler/ghidra_process.hh
+++ b/decompiler/ghidra_process.hh
@@ -101,16 +101,18 @@ public:
 ///
 /// An id is assigned to the program, and an Architecture object for the program
 /// is created and initialized. This must be issued before any other command.
-/// The command expects four XML document parameters:
+/// The command expects four XML document parameters plus one packed document:
 ///   - The processor specification
 ///   - The compiler specification
 ///   - The stripped down \<sleigh> tag describing address spaces for the program
 ///   - The \<coretypes> tag describing the built-in datatypes for the program
+//   - A <doc> containing optional <mapsym><functionshell> entries
 class RegisterProgram : public GhidraCommand {
   string pspec;				///< Processor specification to configure with
   string cspec;				///< Compiler specification to configure with
   string tspec;				///< Configuration (address-spaces) for the Translate object
   string corespec;			///< A description of core data-types for the TypeFactory object
+  string functionShell;			///< Preloaded function shells for prototype overrides
   virtual void loadParameters(void);
   virtual void sendResult(void);
 public:
@@ -118,6 +120,20 @@ public:
   virtual void rawAction(void);
 };
 
+/// \brief Apply a C prototype to a preloaded function.
+///
+/// The function shell is installed during registerProgram so the function's
+/// prototype is symbol-table backed. This command uses the decompiler's own
+/// C declaration parser and invalidates derived analysis after the update.
+class SetFunctionSignature : public GhidraCommand {
+  string functionName;
+  string signature;
+  string functionAddress;
+  virtual void loadParameters(void);
+public:
+  virtual void rawAction(void);
+};
+
 /// \brief Command to \b release all resources associated with a Program (executable) in the decompiler
 ///
 /// The command frees the ArchitectureGhidra object (recursively affecting all resources)
diff --git a/decompiler/ifacedecomp.cc b/decompiler/ifacedecomp.cc
index e62ab6a..6d42a2d 100644
--- a/decompiler/ifacedecomp.cc
+++ b/decompiler/ifacedecomp.cc
@@ -16,6 +16,10 @@
 #include "ifacedecomp.hh"
+#include "ventris_linkage.hh"
+#include "ventris_patterns.hh"
+#include "ventris_flow.hh"
+#include "ventris_constants.hh"
 extern "C" {
 #include <time.h>
 }
 #include "pcodeparse.hh"
 #include "blockaction.hh"
@@ -63,6 +67,10 @@ void IfaceDecompCapability::registerCommands(IfaceStatus *status)
   status->registerCom(new IfcMapconvert(),"map","convert");
   status->registerCom(new IfcMapunionfacet(), "map", "unionfacet");
   status->registerCom(new IfcPrintdisasm(),"disassemble");
+  status->registerCom(new IfcFlow(),"flow");
+  status->registerCom(new IfcLinkage(),"linkage");
+  status->registerCom(new IfcFunctionStarts(),"functionstarts");
+  status->registerCom(new IfcConstants(),"constants");
   status->registerCom(new IfcDecompile(),"decompile");
   status->registerCom(new IfcDump(),"dump");
   status->registerCom(new IfcDumpbinary(),"binary");
@@ -829,6 +837,18 @@ void IfcPrintdisasm::execute(istream &s)
   }
   IfaceAssemblyEmit assem(status->fileoptr,10);
   while(size > 0) {
+    if (dcp->fd != (Funcdata *)0) {
+      const BlockGraph &basic_blocks = dcp->fd->getBasicBlocks();
+      for (int4 i=0;i<basic_blocks.getSize();++i) {
+        const Address block_start = basic_blocks.getBlock(i)->getStart();
+        if (block_start == addr) {
+          *status->fileoptr << "Block ";
+          block_start.printRaw(*status->fileoptr);
+          *status->fileoptr << endl;
+          break;
+        }
+      }
+    }
     int4 sz;
     sz = glb->translate->printAssembly(assem,addr);
     addr = addr + sz;
@@ -836,6 +856,80 @@ void IfcPrintdisasm::execute(istream &s)
   }
 }
 
+void IfcFlow::execute(istream &s)
+
+{
+  if (dcp->conf == (Architecture *)0)
+    throw IfaceExecutionError("No architecture loaded");
+
+  const Sleigh *sleigh = dynamic_cast<const Sleigh *>(dcp->conf->translate);
+  if (sleigh == nullptr)
+    throw IfaceExecutionError("Flow requires a SLEIGH translator");
+  class FlowPcodeEmit : public PcodeEmit {
+  public:
+    int4 count = 0;
+    bool firstBranch = false;
+    bool returnOp = false;
+    virtual void dump(const Address &a,OpCode opc,VarnodeData *outvar,VarnodeData *vars,int4 isize) {
+      if (count++ == 0)
+        firstBranch = opc == CPUI_BRANCH && isize > 0
+          && vars[0].space->getType() == IPTR_PROCESSOR;
+      returnOp |= opc == CPUI_RETURN;
+    }
+  };
+  InstructionFlow flow;
+
+  while (true) {
+    s >> ws;
+    if (s.eof() || s.peek() == -1) break;
+    Address addr;
+    int4 size;
+    try {
+      addr = parse_machaddr(s,size,*dcp->conf->types);
+    } catch(...) {
+      break;
+    }
+    if (addr.isInvalid()) break;
+
+    FlowPcodeEmit emitter;
+    int4 length = 0;
+    try {
+      length = sleigh->oneInstructionFlow(emitter,addr,flow);
+    } catch(BadDataError &err) {
+      *status->fileoptr << "FLOW " << addr.getShortcut() << "0x" << hex << addr.getOffset() << " len=1 fall=none kind=BAD terminal=0 conditional=0 return_op=0 delays= targets=" << '\n';
+      continue;
+    } catch(UnimplError &err) {
+      *status->fileoptr << "FLOW " << addr.getShortcut() << "0x" << hex << addr.getOffset() << " len=1 fall=none kind=UNIMPL terminal=0 conditional=0 return_op=0 delays= targets=" << '\n';
+      continue;
+    }
+
+    InstructionFlow::Type type = flow.type();
+
+    *status->fileoptr << "FLOW " << addr.getShortcut() << "0x" << hex << addr.getOffset() << " len=" << dec << length;
+    if (type.fallthrough) {
+      Address fall = addr + length;
+      *status->fileoptr << " fall=" << fall.getShortcut() << "0x" << hex << fall.getOffset();
+    } else {
+      *status->fileoptr << " fall=none";
+    }
+    *status->fileoptr << " pure_jump=" << (emitter.count == 1 && emitter.firstBranch ? 1 : 0);
+    *status->fileoptr << " no_op=" << (emitter.count == 0 ? 1 : 0);
+    *status->fileoptr << " terminal=" << (type.terminal ? 1 : 0) << " conditional=" << (type.conditional ? 1 : 0);
+    *status->fileoptr << " return_op=" << (emitter.returnOp ? 1 : 0) << " delays=";
+    for(size_t i=0;i<flow.delaySlots.size();++i) {
+      if (i > 0) *status->fileoptr << ",";
+      *status->fileoptr << dec << flow.delaySlots[i];
+    }
+    *status->fileoptr << " kind=" << type.kind << " targets=";
+    for(size_t i=0;i<flow.targets.size();++i) {
+      if (i > 0) *status->fileoptr << ",";
+      *status->fileoptr << flow.targets[i].getShortcut() << "0x" << hex << flow.targets[i].getOffset();
+    }
+    *status->fileoptr << '\n';
+  }
+  status->fileoptr->flush();
+}
+
 /// \class IfcDump
 /// \brief Display bytes in the load image: `dump <address+size>`
 ///
diff --git a/decompiler/ifacedecomp.hh b/decompiler/ifacedecomp.hh
index 5958f67..1eb9d55 100644
--- a/decompiler/ifacedecomp.hh
+++ b/decompiler/ifacedecomp.hh
@@ -203,6 +203,11 @@ public:
   virtual void execute(istream &s);
 };
 
+class IfcFlow : public IfaceDecompCommand {
+public:
+  virtual void execute(istream &s);
+};
+
 class IfcDump : public IfaceDecompCommand {
 public:
   virtual void execute(istream &s);
diff --git a/decompiler/sleigh.hh b/decompiler/sleigh.hh
--- a/decompiler/sleigh.hh
+++ b/decompiler/sleigh.hh
@@ -129,6 +129,9 @@
 /// But, through the \b delay-slot mechanism, build() may recursively visit
 /// additional instructions.
+struct InstructionFlow;
+
 class SleighBuilder : public PcodeBuilder {
   virtual void dump( OpTpl *op );
+  InstructionFlow *instruction_flow;
   AddrSpace *const_space;		///< The constant address space
   AddrSpace *uniq_space;		///< The unique address space
@@ -142,7 +145,7 @@ class SleighBuilder : public PcodeBuilder {
   void generatePointerAdd(PcodeData *op,const VarnodeTpl *vntpl);
   void setUniqueOffset(const Address &addr);	///< Set uniquifying bits for the current instruction
 public:
-  SleighBuilder(ParserWalker *w,DisassemblyCache *dcache,PcodeCacher *pc,AddrSpace *cspc,AddrSpace *uspc,uint4 umask);
+  SleighBuilder(ParserWalker *w,DisassemblyCache *dcache,PcodeCacher *pc,AddrSpace *cspc,AddrSpace *uspc,uint4 umask,InstructionFlow *flow=nullptr);
   virtual void appendBuild(OpTpl *bld,int4 secnum);
   virtual void delaySlot(OpTpl *op);
   virtual void setLabel(OpTpl *op);
@@ -166,6 +169,7 @@ class Sleigh : public SleighBase {
   mutable DisassemblyCache *discache;	///< Cache of recently parsed instructions
   mutable PcodeCacher pcode_cache;	///< Cache of p-code data just prior to emitting
   void clearForDelete(void);		///< Delete the context and disassembly caches
+  int4 translateInstruction(PcodeEmit &emit,const Address &addr,InstructionFlow *flow) const;
 protected:
   ParserContext *obtainContext(const Address &addr,int4 state) const;
   void resolve(ParserContext &pos) const;	///< Generate a parse tree suitable for disassembly
@@ -179,6 +183,7 @@ public:
   virtual void setContextDefault(const string &nm,uintm val);
   virtual void allowContextSet(bool val) const;
   virtual int4 instructionLength(const Address &baseaddr) const;
   virtual int4 oneInstruction(PcodeEmit &emit,const Address &baseaddr) const;
+  int4 oneInstructionFlow(PcodeEmit &emit,const Address &baseaddr,InstructionFlow &flow) const;
   virtual int4 printAssembly(AssemblyEmit &emit,const Address &baseaddr) const;
 };
diff --git a/decompiler/sleigh.cc b/decompiler/sleigh.cc
--- a/decompiler/sleigh.cc
+++ b/decompiler/sleigh.cc
@@ -14,6 +14,7 @@
  * limitations under the License.
  */
 #include "sleigh.hh"
+#include "ventris_flow.hh"
 #include "loadimage.hh"
 
 namespace ghidra {
@@ -221,6 +222,8 @@ void SleighBuilder::dump(OpTpl *op)
 void SleighBuilder::dump(OpTpl *op)
 
 {				// Dump on op through low-level dump interface
 				// filling in dynamic loads and stores if necessary
+  if (instruction_flow != nullptr)
+    instruction_flow->record(op,*walker);
   PcodeData *thisop;
   VarnodeData *invars;
@@ -330,6 +333,7 @@
 SleighBuilder::SleighBuilder(ParserWalker *w,DisassemblyCache *dcache,PcodeCacher *pc,AddrSpace *cspc,
-			     AddrSpace *uspc,uint4 umask)
+			     AddrSpace *uspc,uint4 umask,InstructionFlow *flow)
   : PcodeBuilder(0)
 {
+  instruction_flow = flow;
   walker = w;
   discache = dcache;
@@ -371,6 +375,8 @@ void SleighBuilder::delaySlot(OpTpl *op)
   // Append pcode for an entire instruction (delay slot)
   // in the middle of the current instruction
   ParserWalker *tmp = walker;
   uintb olduniqueoffset = uniqueoffset;
+  InstructionFlow *saved_flow = instruction_flow;
+  instruction_flow = nullptr;	// Slots do not change the parent instruction's flow type.
 
   Address baseaddr = tmp->getAddr();
@@ -395,10 +401,12 @@ void SleighBuilder::delaySlot(OpTpl *op)
   walker = tmp;			// Restore original context
   uniqueoffset = olduniqueoffset;
+  instruction_flow = saved_flow;
 }
 
 void SleighBuilder::setLabel(OpTpl *op)
 
 {
+  if (instruction_flow != nullptr) instruction_flow->note(InstructionFlow::LABEL);
   cache->addLabel( op->getIn(0)->getOffset().getReal()+getLabelBase() );
 }
 
@@ -741,6 +749,17 @@
 int4 Sleigh::oneInstruction(PcodeEmit &emit,const Address &baseaddr) const
+{
+  return translateInstruction(emit,baseaddr,nullptr);
+}
+
+int4 Sleigh::oneInstructionFlow(PcodeEmit &emit,const Address &baseaddr,InstructionFlow &flow) const
+{
+  flow.clear();
+  return translateInstruction(emit,baseaddr,&flow);
+}
+
+int4 Sleigh::translateInstruction(PcodeEmit &emit,const Address &baseaddr,InstructionFlow *flow) const
 
 {
   int4 fallOffset;
   if (alignment != 1) {
     if ((baseaddr.getOffset() % alignment)!=0) {
@@ -761,6 +780,7 @@ int4 Sleigh::oneInstruction(PcodeEmit &emit,const Address &baseaddr) const
       ParserContext *delaypos = obtainContext(pos->getAddr() + fallOffset,ParserContext::pcode);
       delaypos->applyCommits();
       int4 len = delaypos->getLength();
+      if (flow != nullptr) flow->delaySlots.push_back(len);
       fallOffset += len;
       bytecount += len;
     } while(bytecount < pos->getDelaySlot());
@@ -770,6 +790,6 @@ int4 Sleigh::oneInstruction(PcodeEmit &emit,const Address &baseaddr) const
   walker.baseState();
   pcode_cache.clear();
-  SleighBuilder builder(&walker,discache,&pcode_cache,getConstantSpace(),getUniqueSpace(),unique_allocatemask);
+  SleighBuilder builder(&walker,discache,&pcode_cache,getConstantSpace(),getUniqueSpace(),unique_allocatemask,flow);
   try {
     builder.build(walker.getConstructor()->getTempl(),-1);
     pcode_cache.resolveRelatives();
