[package]
name = "ventris-gpui"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"
description = "Native GPUI project and function workspace for Ventris"

[dependencies]
gpui = { version = "0.2.2", default-features = false, features = ["windows-manifest"] }
gpui-component = "0.5.1"
ventris-db = { path = "../../crates/ventris-db" }

[workspace]
